import { List } from '@refinedev/mui';
import { Typography } from '@mui/material';

export const ClaimList = () => {
  return (
    <List>
      <Typography>Liste des réclamations - En développement</Typography>
    </List>
  );
};
