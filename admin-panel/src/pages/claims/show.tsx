import { Show } from '@refinedev/mui';
import { Typography } from '@mui/material';

export const ClaimShow = () => {
  return (
    <Show>
      <Typography>Détails de la réclamation - En développement</Typography>
    </Show>
  );
};
