import { List } from '@refinedev/mui';
import { Typography } from '@mui/material';

export const ItemList = () => {
  return (
    <List>
      <Typography>Liste des articles - En développement</Typography>
    </List>
  );
};
