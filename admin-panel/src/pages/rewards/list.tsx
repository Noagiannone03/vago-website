import { List } from '@refinedev/mui';
import { Typography } from '@mui/material';

export const RewardList = () => {
  return (
    <List>
      <Typography>Liste des récompenses - En développement</Typography>
    </List>
  );
};
