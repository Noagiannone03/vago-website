import { List } from '@refinedev/mui';
import { Typography } from '@mui/material';

export const TripList = () => {
  return (
    <List>
      <Typography>Liste des trajets - En développement</Typography>
    </List>
  );
};
